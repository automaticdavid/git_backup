- name: "Backup Configurations to GIT"
  hosts: ansible
  gather_facts: False
  
  tasks: 
  
  - name: Find latest backup
    find:
      paths: "/backup"
      file_type: directory
    register: found_files

  - debug: 
      var:   "{{ found_files }}"

  - name: Get latest file
    set_fact:
      latest_dir: "{{ found_files.dirs | sort(attribute='mtime') | last }}"

  - name: Copy files to git folder
    copy: 
      src: "{{ latest_dir }}"
      dest: /git_backup


